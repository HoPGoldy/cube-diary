import { FC } from 'react'
import { Edit } from '@react-vant/icons';
import { Space } from 'react-vant'

export const DiaryItemInteract: FC = (props) => {
    return (
        <div className="flex justify-center text-xl">
            <Space>🧑‍🚀💗</Space>
            <Edit fontSize="1.75rem" />
        </div>
    )
}